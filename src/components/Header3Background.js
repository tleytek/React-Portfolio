import styled from 'styled-components';

const Header3Background = styled.div`
  background: rgba(255, 255, 255, 0.5);
  border-radius: 0px 0px 25px 25px;
  align-self: center;
`;

export default Header3Background;
