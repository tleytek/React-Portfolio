import styled from 'styled-components';

const Header1Background = styled.div`
  border-radius: 25px;
  background: rgba(255, 255, 255, 0.5);
  align-self: center;
`;

export default Header1Background;
